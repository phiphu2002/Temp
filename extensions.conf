[pasgo_test]
exten = 1000,1,Verbose(1, "Caller ${CALLERID(num)} dialed.")
 same = n,Stasis(1000)
 same = n,Goto(from-internal,1000,1)
exten = 2000,1,Verbose(1, "Caller ${CALLERID(num)} dialed.")
 same = n,Stasis(2000)
 same = n,Goto(from-internal,2000,1)

[from-internal]
exten = 1000,1,Dial(SIP/1000)
exten = 2000,1,Dial(SIP/2000)
